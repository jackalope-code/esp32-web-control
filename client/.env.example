# Set to true to bypass all authentication (local dev mode)
VITE_NO_AUTH=false

# Server WebSocket connection
VITE_WS_PORT=4000
# VITE_WS_HOST=192.168.1.100  # Uncomment to override (defaults to window.location.hostname)
